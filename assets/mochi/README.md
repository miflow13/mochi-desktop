# Mochi sprite assets

`mochi-sprites.png` is the v0.1 RGBA atlas. Its explicit, non-grid crop rectangles
and animation sequences live in `src/mochi/sprites.py`.

Future sprite sheets can be organized here alongside per-animation directories
such as `idle/`, `walk/`, `sleep/`, `blink/`, and `bounce/`.
